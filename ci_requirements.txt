# Development and CI dependencies.
defusedxml==0.7.1
requests
homeassistant
holidays
pytest
pytest-asyncio
pytest-mock

# Formatting, linting and import sorting, all configured in pyproject.toml.
ruff==0.16.3
